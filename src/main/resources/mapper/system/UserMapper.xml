<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smart.rms.system.mapper.UserMapper">

    <!-- 모든 사용자 조회 -->
    <select id="findAll" resultType="com.smart.rms.system.model.TbUser">
        SELECT USER_ID, USER_NAME, TEL_NO, ADDRESS, EMAIL
        FROM TB_USERS
    </select>

    <!-- ID로 사용자 조회 -->
    <select id="findById" parameterType="String" resultType="com.smart.rms.system.model.TbUser">
        SELECT USER_ID, USER_NAME, TEL_NO, ADDRESS, EMAIL
        FROM TB_USERS
        WHERE USER_ID = #{userId}
    </select>

    <!-- 사용자 등록 -->
    <insert id="save" parameterType="com.smart.rms.system.model.TbUser">
        INSERT INTO TB_USERS (USER_ID, USER_NAME, TEL_NO, ADDRESS, EMAIL)
        VALUES (#{userId}, #{userName}, #{telNo}, #{address}, #{email})
    </insert>

    <!-- 사용자 수정 -->
    <update id="update" parameterType="com.smart.rms.system.model.TbUser">
        UPDATE TB_USERS
        SET USER_NAME = #{userName},
        TEL_NO = #{telNo},
        ADDRESS = #{address},
        EMAIL = #{email}
        WHERE USER_ID = #{userId}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="deleteById" parameterType="String">
        DELETE FROM TB_USERS
        WHERE USER_ID = #{userId}
    </delete>

</mapper>
