<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smart.rms.mapper.MenuMapper">

    <select id="selectAllMenus" resultType="com.smart.rms.model.Menu">
        SELECT MENU_SEQ
             , PARENT_SEQ
             , MENU_NAME
             , MENU_PATH
             , MENU_ICON
             , MENU_ORDER
             , DEL_YN
             , REG_ID
             , REG_DT
             , MOD_ID
             , MOD_DT
        FROM TB_MENU
        WHERE 1=1
          AND DEL_YN = 'N'
        ORDER BY MENU_ORDER ASC, MENU_NAME ASC
    </select>

</mapper>
